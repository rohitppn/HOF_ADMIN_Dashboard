# Auto-deploys the dashboard SPA to GitHub Pages on every push to main.
#
# Before this workflow can succeed:
#   1. Deploy the backend somewhere (Railway/Render/Fly) and note its URL.
#   2. In this repo → Settings → Secrets and variables → Actions → New repository secret:
#        Name:  RAILWAY_API_URL
#        Value: https://<your-app>.up.railway.app       (no trailing slash)
#   3. In this repo → Settings → Pages → Source → set to "GitHub Actions".
#   4. On the backend, set ALLOWED_ORIGINS=https://<username>.github.io
#      (comma-separated if you also want localhost) so CORS lets the dashboard through.

name: Deploy dashboard to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'dashboard/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - name: Build with Pages base path + Railway API URL
        env:
          VITE_BASE: /${{ github.event.repository.name }}/
          VITE_API_BASE: ${{ secrets.RAILWAY_API_URL }}
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
